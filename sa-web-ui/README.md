# Connector Frontend (前端项目)

这是 Connector 平台的前端项目，基于 Vue 3 + Vite + Ant Design Vue 开发。

## 📚 项目简介

`online.base.connector.ui` 是 Connector 平台的管理后台前端，提供了连接器配置、路由管理、多租户管理、权限控制等功能的图形化界面。

主要技术栈：
- **Vue 3**: 渐进式 JavaScript 框架
- **Vite**: 下一代前端构建工具
- **Ant Design Vue**: 企业级 UI 组件库
- **Pinia**: Vue 的专属状态管理库
- **Vue Router**: 路由管理器
- **Axios**: HTTP 客户端

## 🚀 快速开始

### 1. 环境准备
- Node.js >= 18
- npm 或 yarn 或 pnpm

### 2. 安装依赖

```bash
npm install
# 或者
yarn install
# 或者
pnpm install
```

### 3. 运行开发环境

```bash
# 启动本地开发服务 (默认连接本地后端)
npm run dev

# 或者指定 localhost 模式
npm run localhost
```

启动成功后，访问地址通常为：`http://localhost:5173` (具体端口见终端输出)。

### 4. 构建打包

根据不同环境构建项目：

```bash
# 构建测试环境
npm run build:test

# 构建预发布环境
npm run build:pre

# 构建生产环境
npm run build:prod
```

构建产物位于 `dist` 目录下。

## 📂 目录结构

```text
online.base.connector.ui/
├── public/              # 静态资源
├── src/
│   ├── api/             # API 接口封装
│   ├── assets/          # 静态资源 (图片、样式等)
│   ├── components/      # 公共组件
│   ├── config/          # 全局配置
│   ├── constants/       # 常量定义
│   ├── directives/      # 自定义指令
│   ├── i18n/            # 国际化语言包
│   ├── layout/          # 页面布局
│   ├── lib/             # 工具库 (axios, encrypt 等)
│   ├── plugins/         # 插件配置
│   ├── router/          # 路由配置
│   ├── store/           # 状态管理 (Pinia)
│   ├── theme/           # 样式主题
│   ├── utils/           # 通用工具函数
│   ├── views/           # 页面视图
│   ├── App.vue          # 根组件
│   └── main.js          # 入口文件
├── .env.*               # 环境变量配置
├── index.html           # HTML 模板
├── package.json         # 项目依赖与脚本
├── vite.config.js       # Vite 配置
└── README.md            # 项目说明文档
```

## 📝 开发规范
- **代码风格**：项目配置了 ESLint + Prettier，提交代码前请确保无 Lint 错误。
- **组件命名**：推荐使用 PascalCase (如 `RouteList.vue`)。
- **API 管理**：所有接口请求应封装在 `src/api` 目录下，按业务模块划分。

## 🤝 贡献
请遵循团队的代码规范和 Git 工作流进行开发。
