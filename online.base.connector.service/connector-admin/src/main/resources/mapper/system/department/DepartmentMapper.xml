<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhaogang.connector.admin.module.system.department.dao.DepartmentDao">


    <select id="listAll" resultType="com.zhaogang.connector.admin.module.system.department.domain.vo.DepartmentVO">
        SELECT d.*,
               e.actual_name   as managerName,
               parent_department.department_name   as parentName
        FROM t_department d
                 left join t_employee e on d.manager_id = e.employee_id
                 left join t_department parent_department on d.parent_id = parent_department.department_id
        order by d.sort desc
    </select>

    <select id="countSubDepartment" resultType="java.lang.Integer">
        SELECT count(*)
        FROM t_department
        WHERE parent_id = #{departmentId}
    </select>

    <select id="selectDepartmentVO"
            resultType="com.zhaogang.connector.admin.module.system.department.domain.vo.DepartmentVO">
        SELECT d.*,
               e.actual_name   as managerName,
               parent_department.department_name   as parentName
        FROM t_department d
                 left join t_employee e on d.manager_id = e.employee_id
                 left join t_department parent_department on d.parent_id = parent_department.department_id
        where d.department_id = #{departmentId}
    </select>


</mapper>